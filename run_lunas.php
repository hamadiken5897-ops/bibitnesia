<?php require __DIR__.'/vendor/autoload.php'; $app = require_once __DIR__.'/bootstrap/app.php'; $app->make(Illuminate\Contracts\Console\Kernel::class)->bootstrap(); $pesanan = \App\Models\Pesanan::where('id_pesanan', 'ORD-E7V3LNXOTT')->first(); if($pesanan){ $pesanan->status_pesanan = 'diproses'; $pesanan->save(); $pembayaran = \App\Models\Pembayaran::where('id_pesanan', $pesanan->id_pesanan)->first(); if($pembayaran){ $pembayaran->status_validasi = 'dibayar'; $pembayaran->tanggal_pembayaran = now(); $pembayaran->save(); echo 'LUNAS'; } } else { echo 'NOT_FOUND'; }
